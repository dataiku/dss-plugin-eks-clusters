{
    "meta": {
        "label": "AWS connection",
        "pluralLabel": "AWS connections",
        "description": "Preset",
        "icon": "icon-key"
    },

    "defaultDefinableInline": true,
    "defaultDefinableAtProjectLevel": true,

    "params": [
        {
            "name": "accessKey",
            "label": "AWS Access key",
            "description": "If empty, uses AWS credentials from the environment (IAM role, ~/.aws/credentials or AWS_ACCESS_KEY_ID environment variable)",
            "type": "STRING",
            "mandatory": false
        },
        {
            "name": "secretKey",
            "label": "AWS Secret key",
            "description": "If empty, uses AWS credentials from the environment (IAM role, ~/.aws/credentials or AWS_SECRET_ACCESS_KEY environment variable)",
            "type": "PASSWORD",
            "mandatory": false
        },
        {
            "name": "region",
            "label": "AWS Region",
            "description": "If empty, uses AWS region from the environment (~/.aws/config or AWS_DEFAULT_REGION environment variable)",
            "type": "STRING",
            "mandatory": false
        }
    ]
}
